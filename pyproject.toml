[project]
name = "fav-collector"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "selenium>=4.27",
    "requests>=2.32",
]

[tool.ruff]
target-version = "py314"
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
